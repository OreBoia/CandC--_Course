// Libreria standard per input/output (printf, scanf).
#include <stdio.h>

// Punto di ingresso del programma C.
int main(void) {
    // Stampa una frase a schermo e va a capo con \n.
    printf("Ciao, mondo!\n");
    // Restituisce 0 al sistema operativo: esecuzione corretta.
    return 0;
}
